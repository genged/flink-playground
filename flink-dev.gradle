apply from: 'libs.gradle'

dependencies {
    compile group: 'org.apache.flink', name: 'flink-java', version: "${libs.flink}"
    compile group: 'org.apache.flink', name: "flink-streaming-java_${libs.scala}", version: "${libs.flink}"
}